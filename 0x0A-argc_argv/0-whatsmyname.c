#include "main.h"
#include <stdio.h>

/**
 * main - prints the name of the program
 * @argc: count the number of argumnets
 * @argv: the arguments
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
